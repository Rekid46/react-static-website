pipeline {
    agent {
        label 'slave1'
    }
    stages {
        stage("Deploy") {
            steps {
                sh "rm -r /var/www/html/frontend/*"
                sh "mv /home/jenkins/workspace/Frontend/build/* /var/www/html/frontend"
            }
        }
    }
}
