pipeline {
    agent {
        label 'slave1'
    }
    stages {
        stage("Deploy") {
            steps {
                sh "rm -r /var/www/html/*"
                sh "mv /home/jenkins/workspace/FrontendCI/build/* /var/www/html/frontend"
            }
        }
    }
}
