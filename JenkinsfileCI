pipeline {
    agent {
        label 'slave1'
    }
    stages {
        stage("Clear last Build") {
            steps {
                sh "rm -r /home/jenkins/workspace/Frontend/build"
            }
        }
        stage("Creat Build") {
            steps {
                sh "npm run build"
            }
        }       
    }
}
